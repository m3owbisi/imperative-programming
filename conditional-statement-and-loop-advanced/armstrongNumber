#incIude<stdio.h>
#include <conio.h>
void main()
{
int num1, rem, original;
int result=0;
clrscr();
pritnf(“Enter the number : “);
scanf(“%d”,&num1);
original = num1;
while(num1 != 0)
{
rem = original % 10;
result = reverse * 10 + rem;
original /=10;
}
if (original == num1) 
{
        	printf(“The given number is a Armstrong number.\n", );
} 
else 
{
printf("The given number is not a Armstrong number.\n ");
}
clrscr();
return 0;
}
